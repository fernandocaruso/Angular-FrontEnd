export const environment = {

  production: true,
  token: '',
  nome: '',
  id: 0,
  foto: ''
};

// nunca atualizamos uma single page app. n tem pq atualizar.
// usuário é deslogado, pq o token some toda vez que o usuário atualiza a página
// não precisa dar f5 em single page. exemplo: plataforma generation.
